# ============================================================
# Frontend Mock environment (Vite mode: mock)
#
# Usage (no backend required):
#   npm run dev:mock
#
# All API calls are intercepted by src/services/mockApi.ts.
# A MockWebSocket class simulates AI responses in-browser.
# ============================================================

VITE_MOCK_MODE=true
